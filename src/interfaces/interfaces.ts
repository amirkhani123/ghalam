import { IconType } from "react-icons";

interface Ifeatures {
  icon: IconType;
  title: string;
  desc: string;
}
interface Iservices {
  img: string;
  title: string;
}
export type { Ifeatures, Iservices };
